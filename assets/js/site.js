(()=>{'use strict';const reduce=matchMedia('(prefers-reduced-motion: reduce)').matches;const nav=document.getElementById('nav'),menu=document.getElementById('menuBtn'),links=document.getElementById('navLinks');const syncNav=()=>nav&&nav.classList.toggle('solid',scrollY>35);addEventListener('scroll',syncNav,{passive:true});syncNav();if(menu&&links){menu.addEventListener('click',()=>{const open=links.classList.toggle('open');menu.setAttribute('aria-expanded',String(open))});links.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>{links.classList.remove('open');menu.setAttribute('aria-expanded','false')}))}document.getElementById('year').textContent=new Date().getFullYear();const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('in');io.unobserve(e.target)}}),{threshold:.1});document.querySelectorAll('.reveal').forEach(el=>io.observe(el));const routes={site:{label:'CUSTOM WEBSITE',title:'Build an experience that belongs to the business.',text:'Start with the business, audience and action you want visitors to take. Then create a visual system around that instead of choosing a template and filling the boxes.'},idea:{label:'PRODUCT / SAAS PROTOTYPE',title:'Turn the idea into something you can actually touch.',text:'Map the user, workflow and smallest useful version. Then prototype the product so the next decisions come from a working experience instead of an abstract pitch.'},ops:{label:'BUSINESS SYSTEM',title:'Connect the work instead of adding another tab.',text:'Follow the real flow of leads, jobs, people, money and materials. The right answer may be automation, a small internal tool or a larger operating system.'},unknown:{label:'DISCOVERY + PROTOTYPE',title:'Find the friction before naming the software.',text:'Sometimes the valuable first step is simply mapping what is slow, repetitive or unclear. Once the bottleneck is visible, we can decide whether the fix is design, automation or software.'}};const opts=[...document.querySelectorAll('.router-option')],label=document.getElementById('routeLabel'),title=document.getElementById('routeTitle'),text=document.getElementById('routeText');opts.forEach(b=>b.addEventListener('click',()=>{opts.forEach(x=>{x.classList.remove('active');x.setAttribute('aria-selected','false')});b.classList.add('active');b.setAttribute('aria-selected','true');const r=routes[b.dataset.route];if(!r)return;label.textContent=r.label;title.textContent=r.title;text.textContent=r.text}));if(!reduce&&matchMedia('(hover:hover)').matches){const glow=document.getElementById('cursor-glow');let x=innerWidth/2,y=innerHeight/2,tx=x,ty=y;addEventListener('mousemove',e=>{tx=e.clientX;ty=e.clientY;if(glow)glow.style.opacity='1'});addEventListener('mouseleave',()=>{if(glow)glow.style.opacity='0'});const loop=()=>{x+=(tx-x)*.14;y+=(ty-y)*.14;if(glow)glow.style.transform=`translate(${x}px,${y}px) translate(-50%,-50%)`;requestAnimationFrame(loop)};loop();document.querySelectorAll('.magnetic').forEach(btn=>{btn.addEventListener('mousemove',e=>{const r=btn.getBoundingClientRect(),dx=e.clientX-r.left-r.width/2,dy=e.clientY-r.top-r.height/2;btn.style.transform=`translate(${dx*.14}px,${dy*.2}px)`});btn.addEventListener('mouseleave',()=>btn.style.transform='')})}if(!reduce){const c=document.getElementById('particles'),ctx=c&&c.getContext('2d');if(c&&ctx){let w=0,h=0,dpr=1,pts=[];const resize=()=>{dpr=Math.min(devicePixelRatio||1,1.6);w=innerWidth;h=innerHeight;c.width=w*dpr;c.height=h*dpr;c.style.width=w+'px';c.style.height=h+'px';ctx.setTransform(dpr,0,0,dpr,0,0);const count=Math.max(35,Math.min(90,Math.round(w*h/24000)));pts=Array.from({length:count},()=>({x:Math.random()*w,y:Math.random()*h,r:.45+Math.random()*1.25,v:.08+Math.random()*.28,a:.16+Math.random()*.36,drift:(Math.random()-.5)*.08}))};resize();addEventListener('resize',resize);const draw=()=>{ctx.clearRect(0,0,w,h);for(const p of pts){p.y-=p.v;p.x+=p.drift;if(p.y<-5){p.y=h+5;p.x=Math.random()*w}if(p.x<-5)p.x=w+5;if(p.x>w+5)p.x=-5;ctx.beginPath();ctx.arc(p.x,p.y,p.r,0,Math.PI*2);ctx.fillStyle=`rgba(145,188,226,${p.a})`;ctx.fill()}requestAnimationFrame(draw)};draw()}const pc=document.getElementById('plasma'),px=pc&&pc.getContext('2d');if(pc&&px){let w=0,h=0,dpr=1,t=0,mx=.55,my=.42,tmx=.55,tmy=.42;const resize=()=>{dpr=Math.min(devicePixelRatio||1,1.5);w=pc.clientWidth;h=pc.clientHeight;pc.width=w*dpr;pc.height=h*dpr;px.setTransform(dpr,0,0,dpr,0,0)};resize();addEventListener('resize',resize);addEventListener('mousemove',e=>{const r=pc.getBoundingClientRect();tmx=(e.clientX-r.left)/Math.max(r.width,1);tmy=(e.clientY-r.top)/Math.max(r.height,1)});const draw=()=>{t+=.0048;mx+=(tmx-mx)*.035;my+=(tmy-my)*.035;px.clearRect(0,0,w,h);const cols=23,rows=17,sx=w/cols,sy=h/rows;for(let i=0;i<=cols;i++){px.beginPath();for(let j=0;j<=rows;j++){const bx=i*sx,by=j*sy,dx=bx/w-mx,dy=by/h-my,pull=Math.exp(-(dx*dx+dy*dy)*6)*38,x=bx+Math.sin(i*.52+j*.25+t*2)*12+dx*pull,y=by+Math.cos(j*.58-i*.17-t*1.7)*9+dy*pull;j?px.lineTo(x,y):px.moveTo(x,y)}px.strokeStyle=`rgba(${105+Math.round(i/cols*35)},${158+Math.round(i/cols*30)},220,.11)`;px.lineWidth=1;px.stroke()}requestAnimationFrame(draw)};draw()}}})();