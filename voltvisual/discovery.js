(()=>{
'use strict';const host=document.createElement('section');host.className='loop-lesson section-shell';host.id='signalLesson';
host.innerHTML=`<header><span>FIRST SIGNAL / TRY IT IN TWO MINUTES</span><h2>One current.<br><em>A whole measurement.</em></h2><p>A level transmitter turns a tank measurement into a 4–20 mA signal. The controller converts that signal back into engineering units. Move the level, then introduce a fault.</p></header><div class="loop-bench"><div class="loop-process"><div class="loop-tank" aria-hidden="true"><i id="tankFill"></i><b id="tankPercent">50%</b></div><label for="tankLevel">Actual tank level</label><input id="tankLevel" type="range" min="0" max="100" value="50"><output id="tankLevelOut">50%</output></div><div class="loop-signal"><label for="loopFault">Signal condition</label><select id="loopFault"><option value="normal">Healthy loop</option><option value="open">Open wire (simulated)</option><option value="scale">Wrong controller scale: 0–200%</option></select><div class="loop-readouts"><div><span>Received current</span><b id="loopCurrent"></b></div><div><span>Controller display</span><b id="loopDisplay"></b></div></div><p id="loopMeaning" aria-live="polite"></p><code id="loopFormula"></code></div></div><div class="loop-check"><h3>The meter reads 12 mA. What can you conclude?</h3><div><button type="button" data-loop-answer="0">The signal is halfway through its span</button><button type="button" data-loop-answer="1">The tank is definitely half full</button><button type="button" data-loop-answer="2">The signal is at 60%</button></div><p id="loopFeedback" aria-live="polite">Think about what the wire tells you—and what still needs verification.</p></div><details><summary>Why 4 mA instead of zero?</summary><p>For a linear 4–20 mA signal, the usable span is 16 mA. Four mA represents the configured lower range value. Zero mA is outside that normal range and can indicate a broken loop or lost supply; it does not prove which component failed. Actual instruments have their own fault signaling and range settings.</p><a href="https://www.fluke.com/en-us/learn/blog/calibration/what-is-a-4-20-ma-current-loop" target="_blank" rel="noopener">Fluke: the 4–20 mA current loop ↗</a><p>This is a virtual signal model. Physical measurement and service require the equipment procedure and appropriate training.</p></details>`;
const anchor=document.querySelector('#machine');if(!anchor)return;anchor.before(host);const $=s=>host.querySelector(s);
function draw(){const level=+$('#tankLevel').value,fault=$('#loopFault').value,mA=fault==='open'?0:4+16*level/100;$('#tankFill').style.height=level+'%';$('#tankPercent').textContent=level+'%';$('#tankLevelOut').textContent=level+'%';$('#loopCurrent').textContent=mA.toFixed(1)+' mA';$('#loopDisplay').textContent=fault==='open'?'Invalid signal':Math.round(level*(fault==='scale'?2:1))+'%';$('#loopMeaning').textContent=fault==='open'?'The tank has not emptied. The controller has lost a valid signal. Treat the measurement as unknown.':fault==='scale'?'The transmitter signal is valid, but the receiver is configured for twice the intended range. Check scaling before blaming the sensor.':'The transmitter and controller agree because both use the same 0–100% range.';$('#loopFormula').textContent=fault==='open'?'0 mA lies outside the 4–20 mA measurement range.':`(${mA.toFixed(1)} − 4) ÷ 16 × ${fault==='scale'?200:100} = ${level*(fault==='scale'?2:1)}%`;host.dataset.fault=fault}
$('#tankLevel').addEventListener('input',draw);$('#loopFault').addEventListener('change',draw);host.querySelectorAll('[data-loop-answer]').forEach(b=>b.addEventListener('click',()=>{$('#loopFeedback').textContent=b.dataset.loopAnswer==='0'?'Correct: (12 − 4) ÷ 16 = 50% of the configured signal span. Sensor setup, actual process and controller scaling still need to agree.':b.dataset.loopAnswer==='1'?'That is true only if the sensor is correct and both devices use the intended range. A signal value alone does not prove the physical level.':'Subtract the 4 mA starting point first: (12 − 4) ÷ (20 − 4) = 50%, not 12 ÷ 20.'}));draw();
})();
