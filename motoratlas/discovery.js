(()=>{
'use strict';
const host=document.createElement('section');host.id='ratioLab';host.className='ratio-lab shell';
host.innerHTML=`<div class="ratio-heading"><span>DRIVE LAB / 01</span><h2>Trade speed for<br><em>turning force.</em></h2><p>A gearbox does not create power. Change the reduction and see where speed, torque and lost energy go.</p></div><div class="ratio-console"><div class="ratio-settings"><label>Reduction ratio <output id="ratioLabel">3:1</output><input id="gearRatio" type="range" min="1" max="6" step="0.5" value="3"></label><label>Input speed <output id="rpmLabel">3,000 rpm</output><input id="inputRpm" type="range" min="600" max="6000" step="100" value="3000"></label><label>Gearbox efficiency <output id="effLabel">90%</output><input id="gearEff" type="range" min="60" max="100" step="1" value="90"></label><p>Input torque is held at 100 N·m. This is an illustrative steady-state gearbox, not a prediction for a specific vehicle.</p></div><div class="ratio-results"><div class="ratio-shafts" aria-hidden="true"><div><i></i><span>INPUT SHAFT</span></div><div class="ratio-output-shaft"><i></i><span>OUTPUT SHAFT</span></div></div><dl><div><dt>Output speed</dt><dd id="ratioSpeed"></dd></div><div><dt>Output torque</dt><dd id="ratioTorque"></dd></div><div><dt>Power delivered</dt><dd id="ratioPower"></dd></div><div><dt>Power lost</dt><dd id="ratioLoss"></dd></div></dl><p id="ratioExplain" aria-live="polite"></p></div></div><div class="ratio-question"><h3>Predict before you change it.</h3><p>At the same input speed and efficiency, doubling the reduction ratio will…</p><div><button type="button" data-ratio-answer="0">Double speed and torque</button><button type="button" data-ratio-answer="1">Halve speed, double torque</button><button type="button" data-ratio-answer="2">Halve both</button></div><p id="ratioFeedback" aria-live="polite">Choose an answer, then try it with the slider.</p></div><details><summary>Show the math and source</summary><p>Output rpm = input rpm ÷ reduction ratio. Output torque = input torque × ratio × efficiency. Power in watts = torque × rpm × 2π ÷ 60. Real gearboxes also have torque and thermal limits, changing efficiency and transient loads.</p><a href="https://blog.orientalmotor.com/gearhead-selection-for-stepper-motors" target="_blank" rel="noopener">Oriental Motor: speed, torque and efficiency ↗</a></details>`;
const anchor=document.querySelector('#diagnostics');if(!anchor)return;anchor.before(host);
const $=s=>host.querySelector(s);function draw(){const ratio=+$('#gearRatio').value,rpm=+$('#inputRpm').value,eff=+$('#gearEff').value/100,power=100*rpm*2*Math.PI/60/1000;$('#ratioLabel').textContent=ratio+':1';$('#rpmLabel').textContent=rpm.toLocaleString()+' rpm';$('#effLabel').textContent=Math.round(eff*100)+'%';$('#ratioSpeed').textContent=Math.round(rpm/ratio).toLocaleString()+' rpm';$('#ratioTorque').textContent=Math.round(100*ratio*eff)+' N·m';$('#ratioPower').textContent=(power*eff).toFixed(1)+' kW';$('#ratioLoss').textContent=(power*(1-eff)).toFixed(1)+' kW';$('#ratioExplain').textContent=`The output turns at 1/${ratio} of the input speed. ${Math.round(eff*100)}% of input power reaches it; the rest is lost, mainly as heat.`;host.style.setProperty('--shaft-speed',2*3000/rpm+'s');host.style.setProperty('--output-speed',2*3000/rpm*ratio+'s')}
host.querySelectorAll('input').forEach(x=>x.addEventListener('input',draw));host.querySelectorAll('[data-ratio-answer]').forEach(b=>b.addEventListener('click',()=>{host.querySelectorAll('[data-ratio-answer]').forEach(x=>x.setAttribute('aria-pressed',String(x===b)));$('#ratioFeedback').textContent=b.dataset.ratioAnswer==='1'?'Exactly. Twice the ratio gives half the speed and twice the torque. With efficiency unchanged, output power stays the same.':'Follow the trade: speed is divided by the ratio; torque is multiplied by it. Double the ratio: half the speed, twice the torque.'}));draw();
})();
